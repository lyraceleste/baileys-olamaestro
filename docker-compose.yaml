services:
  whatsmeow:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - whatsmeow-data:/root
    environment:
      - N8N_WEBHOOK=http://n8n-lg4s0cw48w4g08gwk0w4o8g8:5678/webhook/whatsapp
    restart: unless-stopped
    networks:
      - coolify

networks:
  coolify:
    external: true

volumes:
  whatsmeow-data:
